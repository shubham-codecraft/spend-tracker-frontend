services:
  - type: web
    name: spend-tracker-frontend
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
